export interface NavItem {
  label: string;
  href: string;
}

export interface FeatureProps {
  title: string;
  description: string;
  image?: string;
}

export interface PillarProps {
  title: string;
  description: string;
}